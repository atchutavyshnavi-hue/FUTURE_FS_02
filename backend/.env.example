# Port the API server listens on
PORT=4000

# MongoDB connection string (get this from MongoDB Atlas - see README for setup steps)
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/?retryWrites=true&w=majority

# Secret used to sign admin JWT sessions - change this to a long random string in production
JWT_SECRET=replace-this-with-a-long-random-secret

# How long an admin login session stays valid
JWT_EXPIRES_IN=8h

# Default admin account created the first time the server starts (only if no admin exists yet)
DEFAULT_ADMIN_EMAIL=atchutavyshnavi@gmail.com
DEFAULT_ADMIN_PASSWORD=Ab1@2345

# Comma-separated list of origins allowed to call the API (your frontend URL(s))
CORS_ORIGIN=*